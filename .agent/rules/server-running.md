---
trigger: always_on
---

When you want to run the server. ALWAYS CHECK FIRST if the app is currently running, you can check common ports (3000, 3001, 3002) or use a command to see if the dev server is running. Remember we are using bun and not npm